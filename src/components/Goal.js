import React from 'react';


// will need goals to be passed in from goal list component

const Goal = () => {

    return(
        <div>
            <h2>Goal</h2>
        </div>
    );
};

export default Goal;